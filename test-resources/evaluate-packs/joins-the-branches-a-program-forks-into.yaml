# SPDX-FileCopyrightText: Copyright (c) 2025 Objectionary.com
# SPDX-License-Identifier: MIT
---
# yamllint disable rule:line-length
# The same fork as the pack beside this one, answering the join of its two
# branches instead of the first of them, with one more firing standing after
# it: the branch point stays in the program, since the 'plus( 6 )' below the
# fork reads the symbol the two branches were joined into and not the value of
# either. The line saying what that symbol stands for is what ties it to the
# two, and the firing of a branch stands one level deeper, under the fork that
# asked for it.
symbolic: |
  - λ: L_number_gt
    dataize:
      𝛿1: $.ρ
      𝛿2: $.x
    𝑛: Φ.bool( φ ↦ ⟦ λ ⤍ 𝜎 ⟧ )
  - λ: L_number_plus
    dataize:
      𝛿1: $.ρ
      𝛿2: $.x
    𝑛: Φ.number( φ ↦ ⟦ λ ⤍ 𝜎 ⟧ )
  - λ: L_fork
    dataize:
      𝛿1: $.ρ
    morph:
      𝑛1: $.then
      𝑛2: $.else
    symbolize:
      𝑛3: 𝑛1
      𝑛4: 𝑛2
    join:
      𝑛5: [𝑛3, 𝑛4]
    𝑛: 𝑛5
location: 'Q.@'
deep: true
input: >
  ⟦ bytes(φ) ↦ ⟦⟧,
    bool(φ) ↦ ⟦ if ↦ ⟦ then ↦ ∅, else ↦ ∅, λ ⤍ L_fork ⟧ ⟧,
    number(φ) ↦ ⟦
      gt(x) ↦ ⟦ λ ⤍ L_number_gt ⟧,
      plus(x) ↦ ⟦ λ ⤍ L_number_plus ⟧ ⟧,
    @ ↦ 5.gt( 0 ).if( 1.plus( 2 ), 3.plus( 4 ) ).plus( 6 ) ⟧
result: '⟦ φ ↦ ⟦ λ ⤍ 𝜎5 ⟧, gt(x) ↦ ⟦ λ ⤍ L_number_gt ⟧, plus(x) ↦ ⟦ λ ⤍ L_number_plus ⟧, ρ ↦ ⟦ bytes(φ) ↦ ⟦⟧, bool(φ) ↦ ⟦ if(then, else) ↦ ⟦ λ ⤍ L_fork ⟧ ⟧, number(φ) ↦ ⟦ gt(x) ↦ ⟦ λ ⤍ L_number_gt ⟧, plus(x) ↦ ⟦ λ ⤍ L_number_plus ⟧ ⟧, φ ↦ 5.gt( 0 ).if( 1.plus( 2 ), 3.plus( 4 ) ).plus( 6 ) ⟧ ⟧'
protocol: |
  𝕄(Φ.φ)
    𝔼(L_number_gt)  # 𝕄(Φ.φ)
      𝛿1.1 := 40-14-00-00-00-00-00-00  # 𝔻(ξ.ρ)
      𝛿2.1 := 00-00-00-00-00-00-00-00  # 𝔻(ξ.x)
      𝑛.1.1 := Φ.bool( φ ↦ ⟦ λ ⤍ 𝜎1 ⟧ )  # 𝑛
      𝑛.1.2 := ⟦ φ ↦ ⟦ λ ⤍ 𝜎1 ⟧, if(then, else) ↦ ⟦ λ ⤍ L_fork ⟧, ρ ↦ ⟦ bytes(φ) ↦ ⟦⟧, bool(φ) ↦ ⟦ if(then, else) ↦ ⟦ λ ⤍ L_fork ⟧ ⟧, number(φ) ↦ ⟦ gt(x) ↦ ⟦ λ ⤍ L_number_gt ⟧, plus(x) ↦ ⟦ λ ⤍ L_number_plus ⟧ ⟧, φ ↦ 5.gt( 0 ).if( 1.plus( 2 ), 3.plus( 4 ) ).plus( 6 ) ⟧ ⟧  # 𝕄(𝑛.1.1)
    𝔼(L_fork)  # 𝕄(Φ.φ)
      𝛿1.2 := 𝔻(⟦ λ ⤍ 𝜎1 ⟧)  # 𝔻(ξ.ρ)
      𝔼(L_number_plus)  # 𝕄(Φ.a🌵3)
        𝛿1.3 := 3F-F0-00-00-00-00-00-00  # 𝔻(ξ.ρ)
        𝛿2.3 := 40-00-00-00-00-00-00-00  # 𝔻(ξ.x)
        𝑛.3.1 := Φ.number( φ ↦ ⟦ λ ⤍ 𝜎2 ⟧ )  # 𝑛
        𝑛.3.2 := ⟦ φ ↦ ⟦ λ ⤍ 𝜎2 ⟧, gt(x) ↦ ⟦ λ ⤍ L_number_gt ⟧, plus(x) ↦ ⟦ λ ⤍ L_number_plus ⟧, ρ ↦ ⟦ a🌵3 ↦ 1.plus( α0 ↦ 2, ρ ↦ ⟦ then ↦ 1.plus( 2 ), else ↦ 3.plus( 4 ), ρ ↦ ⟦ φ ↦ ⟦ λ ⤍ 𝜎1 ⟧, if(then, else) ↦ ⟦ λ ⤍ L_fork ⟧, ρ ↦ ⟦ bytes(φ) ↦ ⟦⟧, bool(φ) ↦ ⟦ if(then, else) ↦ ⟦ λ ⤍ L_fork ⟧ ⟧, number(φ) ↦ ⟦ gt(x) ↦ ⟦ λ ⤍ L_number_gt ⟧, plus(x) ↦ ⟦ λ ⤍ L_number_plus ⟧ ⟧, φ ↦ 5.gt( 0 ).if( 1.plus( 2 ), 3.plus( 4 ) ).plus( 6 ) ⟧ ⟧ ⟧ ), bytes(φ) ↦ ⟦⟧, bool(φ) ↦ ⟦ if(then, else) ↦ ⟦ λ ⤍ L_fork ⟧ ⟧, number(φ) ↦ ⟦ gt(x) ↦ ⟦ λ ⤍ L_number_gt ⟧, plus(x) ↦ ⟦ λ ⤍ L_number_plus ⟧ ⟧, φ ↦ 5.gt( 0 ).if( 1.plus( 2 ), 3.plus( 4 ) ).plus( 6 ) ⟧ ⟧  # 𝕄(𝑛.3.1)
      𝑛1.2 := 𝑛.3.2  # 𝕄(ξ.then)
      𝔼(L_number_plus)  # 𝕄(Φ.a🌵6)
        𝛿1.4 := 40-08-00-00-00-00-00-00  # 𝔻(ξ.ρ)
        𝛿2.4 := 40-10-00-00-00-00-00-00  # 𝔻(ξ.x)
        𝑛.4.1 := Φ.number( φ ↦ ⟦ λ ⤍ 𝜎3 ⟧ )  # 𝑛
        𝑛.4.2 := ⟦ φ ↦ ⟦ λ ⤍ 𝜎3 ⟧, gt(x) ↦ ⟦ λ ⤍ L_number_gt ⟧, plus(x) ↦ ⟦ λ ⤍ L_number_plus ⟧, ρ ↦ ⟦ a🌵6 ↦ 3.plus( α0 ↦ 4, ρ ↦ ⟦ then ↦ 1.plus( 2 ), else ↦ 3.plus( 4 ), ρ ↦ ⟦ φ ↦ ⟦ λ ⤍ 𝜎1 ⟧, if(then, else) ↦ ⟦ λ ⤍ L_fork ⟧, ρ ↦ ⟦ bytes(φ) ↦ ⟦⟧, bool(φ) ↦ ⟦ if(then, else) ↦ ⟦ λ ⤍ L_fork ⟧ ⟧, number(φ) ↦ ⟦ gt(x) ↦ ⟦ λ ⤍ L_number_gt ⟧, plus(x) ↦ ⟦ λ ⤍ L_number_plus ⟧ ⟧, φ ↦ 5.gt( 0 ).if( 1.plus( 2 ), 3.plus( 4 ) ).plus( 6 ) ⟧ ⟧ ⟧ ), bytes(φ) ↦ ⟦⟧, bool(φ) ↦ ⟦ if(then, else) ↦ ⟦ λ ⤍ L_fork ⟧ ⟧, number(φ) ↦ ⟦ gt(x) ↦ ⟦ λ ⤍ L_number_gt ⟧, plus(x) ↦ ⟦ λ ⤍ L_number_plus ⟧ ⟧, φ ↦ 5.gt( 0 ).if( 1.plus( 2 ), 3.plus( 4 ) ).plus( 6 ) ⟧ ⟧  # 𝕄(𝑛.4.1)
      𝑛2.2 := 𝑛.4.2  # 𝕄(ξ.else)
      𝑛3.2 := 𝑛1.2  # 𝑛1
      𝑛4.2 := 𝑛2.2  # 𝑛2
      𝔻(⟦ λ ⤍ 𝜎4 ⟧) ∈ { 𝔻(⟦ λ ⤍ 𝜎2 ⟧), 𝔻(⟦ λ ⤍ 𝜎3 ⟧) }
      𝑛5.2 := ⟦ φ ↦ ⟦ λ ⤍ 𝜎4 ⟧, gt(x) ↦ ⟦ λ ⤍ L_number_gt ⟧, plus(x) ↦ ⟦ λ ⤍ L_number_plus ⟧, ρ ↦ ⟦ a🌵3 ↦ 1.plus( α0 ↦ 2, ρ ↦ ⟦ then ↦ 1.plus( 2 ), else ↦ 3.plus( 4 ), ρ ↦ ⟦ φ ↦ ⟦ λ ⤍ 𝜎1 ⟧, if(then, else) ↦ ⟦ λ ⤍ L_fork ⟧, ρ ↦ ⟦ bytes(φ) ↦ ⟦⟧, bool(φ) ↦ ⟦ if(then, else) ↦ ⟦ λ ⤍ L_fork ⟧ ⟧, number(φ) ↦ ⟦ gt(x) ↦ ⟦ λ ⤍ L_number_gt ⟧, plus(x) ↦ ⟦ λ ⤍ L_number_plus ⟧ ⟧, φ ↦ 5.gt( 0 ).if( 1.plus( 2 ), 3.plus( 4 ) ).plus( 6 ) ⟧ ⟧ ⟧ ), bytes(φ) ↦ ⟦⟧, bool(φ) ↦ ⟦ if(then, else) ↦ ⟦ λ ⤍ L_fork ⟧ ⟧, number(φ) ↦ ⟦ gt(x) ↦ ⟦ λ ⤍ L_number_gt ⟧, plus(x) ↦ ⟦ λ ⤍ L_number_plus ⟧ ⟧, φ ↦ 5.gt( 0 ).if( 1.plus( 2 ), 3.plus( 4 ) ).plus( 6 ) ⟧ ⟧  # [𝑛3, 𝑛4]
      𝑛.2.1 := 𝑛5.2  # 𝑛
      𝑛.2.2 := 𝑛5.2  # 𝕄(𝑛.2.1)
    𝔼(L_number_plus)  # 𝕄(Φ.φ)
      𝛿1.5 := 𝔻(⟦ λ ⤍ 𝜎4 ⟧)  # 𝔻(ξ.ρ)
      𝛿2.5 := 40-18-00-00-00-00-00-00  # 𝔻(ξ.x)
      𝑛.5.1 := Φ.number( φ ↦ ⟦ λ ⤍ 𝜎5 ⟧ )  # 𝑛
      𝑛.5.2 := ⟦ φ ↦ ⟦ λ ⤍ 𝜎5 ⟧, gt(x) ↦ ⟦ λ ⤍ L_number_gt ⟧, plus(x) ↦ ⟦ λ ⤍ L_number_plus ⟧, ρ ↦ ⟦ bytes(φ) ↦ ⟦⟧, bool(φ) ↦ ⟦ if(then, else) ↦ ⟦ λ ⤍ L_fork ⟧ ⟧, number(φ) ↦ ⟦ gt(x) ↦ ⟦ λ ⤍ L_number_gt ⟧, plus(x) ↦ ⟦ λ ⤍ L_number_plus ⟧ ⟧, φ ↦ 5.gt( 0 ).if( 1.plus( 2 ), 3.plus( 4 ) ).plus( 6 ) ⟧ ⟧  # 𝕄(𝑛.5.1)
