Φ ↦ ⟦
  org -> ⟦
    eolang -> ⟦
      x ↦ ⟦
        y ↦ ⟦ Δ ⤍ 00-01 ⟧,
        right ↦ ⟦
          λ ⤍ Func,
          x ↦ ∅,
          ρ ↦ ⟦⟧
        ⟧,
        ρ ↦ ∅,
        φ ↦ ∅,
        void ↦ ∅,
        global ↦ Φ,
        this ↦ ξ,
        this-disp-label ↦ ξ.org,
        this-disp-idx ↦ ξ.α1,
        global-disp-label ↦ Φ.org,
        global-disp-idx ↦ Φ.α1,
        formation-disp ↦ ⟦ attr ↦ ∅ ⟧.attr,
        org-eolang ↦ Φ.org.eolang,
        nested ↦ ⟦ attr ↦ ∅ ⟧.attr.other(ξ, Φ),
        appl1 ↦ Φ.org(
          as ↦ Φ.eolang
        ),
        appl2 ↦ Φ.eolang.attr(ξ, Φ, ⟦⟧)
      ⟧,
      λ ⤍ Package
    ⟧,
    λ ⤍ Package
  ⟧
⟧