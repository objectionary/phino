# SPDX-FileCopyrightText: Copyright (c) 2025 Objectionary.com
# SPDX-License-Identifier: MIT
---
input: |
  ⟦
    str -> "hello",
    ch -> "h",
    uni -> "\u0001\u0002",
    empty -> "",
    new-line -> "h\ne"
  ⟧
output: |-
  ⟦
    str ↦ Φ.string(
      as-bytes ↦ Φ.bytes(
        φ ↦ ⟦ Δ ⤍ 68-65-6C-6C-6F ⟧
      )
    ),
    ch ↦ Φ.string(
      as-bytes ↦ Φ.bytes(
        φ ↦ ⟦ Δ ⤍ 68- ⟧
      )
    ),
    uni ↦ Φ.string(
      as-bytes ↦ Φ.bytes(
        φ ↦ ⟦ Δ ⤍ 01-02 ⟧
      )
    ),
    empty ↦ Φ.string(
      as-bytes ↦ Φ.bytes(
        φ ↦ ⟦ Δ ⤍ -- ⟧
      )
    ),
    new-line ↦ Φ.string(
      as-bytes ↦ Φ.bytes(
        φ ↦ ⟦ Δ ⤍ 68-0A-65 ⟧
      )
    )
  ⟧
