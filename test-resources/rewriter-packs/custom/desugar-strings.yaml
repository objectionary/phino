# SPDX-FileCopyrightText: Copyright (c) 2025 Objectionary.com
# SPDX-License-Identifier: MIT
---
input: |
  {⟦
    str -> "hello",
    ch -> "h",
    uni -> "\u0001\u0002",
    empty -> "",
    new-line -> "h\ne"
  ⟧}
output: |-
  Φ ↦ ⟦
    str ↦ Φ.string(
      α0 ↦ Φ.bytes(
        α0 ↦ ⟦ Δ ⤍ 68-65-6C-6C-6F ⟧
      )
    ),
    ch ↦ Φ.string(
      α0 ↦ Φ.bytes(
        α0 ↦ ⟦ Δ ⤍ 68- ⟧
      )
    ),
    uni ↦ Φ.string(
      α0 ↦ Φ.bytes(
        α0 ↦ ⟦ Δ ⤍ 01-02 ⟧
      )
    ),
    empty ↦ Φ.string(
      α0 ↦ Φ.bytes(
        α0 ↦ ⟦ Δ ⤍ -- ⟧
      )
    ),
    new-line ↦ Φ.string(
      α0 ↦ Φ.bytes(
        α0 ↦ ⟦ Δ ⤍ 68-0A-65 ⟧
      )
    )
  ⟧
