# SPDX-FileCopyrightText: Copyright (c) 2025 Objectionary.com
# SPDX-License-Identifier: MIT
---
# yamllint disable rule:line-length
name: latex
'on':
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  cabal:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - id: setup
        uses: haskell-actions/setup@v2.11.0
        with:
          ghc-version: '9.6.7'
          cabal-version: '3.12.1.0'
      - uses: zauguin/install-texlive@v4.4.0
        with:
          packages: scheme-basic eolang mathtools
          texlive_version: 2026
      - uses: actions/cache@v6
        with:
          path: |
            ${{ steps.setup.outputs.ghc-path }}
            ${{ steps.setup.outputs.cabal-path }}
            ${{ steps.setup.outputs.cabal-store }}
            dist-newstyle
          key: ${{ runner.os }}-cabal-${{ hashFiles('**/*.cabal') }}-${{ hashFiles('cabal.project', 'cabal.project.freeze') }}
          restore-keys: ${{ runner.os }}-cabal-
      - run: cabal update
      - run: cabal build
      - shell: bash
        run: |
          set -e -o pipefail
          cabal build
          e=$(cabal list-bin exe:phino)
          if [ -z "${e}" ]; then exit 1; fi
          echo "phino found at: '${e}'."
          (
            printf '\\documentclass{article}\n'
            printf '\\usepackage{mathtools}\n'
            printf '\\usepackage{eolang}\n'
            printf '\\newcommand\\ctx[2]{#1 #2}\n'
            printf '\\newcommand\\phinoEvaluate[1]{\\mathbb{E}( #1 )}\n'
            printf '\\newcommand\\phinoMorph[1]{\\mathbb{M}( #1 )}\n'
            printf '\\newcommand\\phinoGlobal[1]{\\Phi( #1 )}\n'
            printf '\\newcommand\\indexof[1]{#1}\n'
            printf '\\newcommand\\isnormal[1]{#1}\n'
            printf '\\newcommand\\phinoMorphingRule[5]{#1 $#2$ $#3$ $#4$ $#5$}\n'
            printf '\\newcommand\\phinoNormalizationRule[5]{#1 $#2$ $#3$ $#4$ $#5$}\n'
            printf '\\newcommand\\phinoDataizationRule[5]{#1 $#2$ $#3$ $#4$ $#5$}\n'
            printf '\\begin{document}\n'
            "${e}" explain --normalize
            printf '{[[ x -> [[ y -> ? ]].y ]]}' | "${e}" rewrite --normalize --output=latex --sequence
            printf '{[[ x -> [[ y -> ? ]].y ]]}' | "${e}" dataize --output=latex --sequence --quiet
            printf '\\end{document}\n'
          ) > article.tex
          pdflatex -shell-escape -interaction=errorstopmode -halt-on-error article
